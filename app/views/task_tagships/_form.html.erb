<%= form_for(@task_tagship) do |f| %>
  <% if @task_tagship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_tagship.errors.count, "error") %> prohibited this task_tagship from being saved:</h2>

      <ul>
      <% @task_tagship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task_id %><br />
    <%= f.number_field :task_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br />
    <%= f.number_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
